# Provider implementation log

2026-09-05. The user authorized implementation of the plan. Existing Astra changes were preserved.

1. Reread reference contracts and existing patches. Existing code knows only ChatGPT accounts/plans; its eligibility calculation cannot govern external model selection.
2. The provider spike used an empty temporary CODEX_HOME, the official installed app-server, and a loopback-only mock. thread/start.config accepted model_providers.cxp-external. With modelProvider=cxp-external, model=cxp-test-model, and low effort, the mock received /v1/responses. The assistant returned PROVIDER_OK with completed/error=null and no ChatGPT Authorization header. Evidence: `.build/provider-implementation/spike.json`. Thread overrides work without changing shared configuration or patching child-process startup.
3. AST extraction located the AppServerManager constructor/requestClient contract, model/list calls, thread creation, and native account routing. The external catalog and thread route attach at the request-client boundary. External models retain distinct slugs.
4. In the live candidate, model/list returned only low/high for the connected local model. The actual model row was selected with scrolling and CDP mouse input. At this stage, the profile-menu provider entry opened a separate secure window. Live catalog refresh found 37 providers/175 models, or 41 options including local/custom fallbacks.
5. Through that app's request client, thread/start → turn/start → thread/read returned cxp-external, high effort, EXTERNAL_OK, completed/error=null. The mock received upstream model cxp-test-model and no ChatGPT account header. Evidence: live-turn.json and live-request.json. This controlled local end-to-end test used no paid provider key.
6. Contract review found three issues: an SSE CRLF delimiter could split across network chunks, the model form lacked an upstream ID field, and Go's usage API reported percentage windows. The parser, form, and quota adapter were corrected. Unknown quota is not presented as zero or unlimited.
7. Forced the complete catalog-download path in a separate directory: resolve SHA → check MIT license → read only config JSON from the archive → atomically write the registry. Result remained 37/175; the connection file was byte-identical. Evidence: registry-refresh.json.
8. The compact UI uses a 78px header, 47px tab row, two-column connection/editor layout, compact model rows, and adjacent quota summaries. CDP cache-disabled reload verified new CSS/JS; compact-editor.png. The first location.reload was blocked by navigation protection, so its stale screenshot was not treated as evidence.
9. Tested Chat/Messages tool streams, fragmented JSON arguments, and CRLF boundaries. Provider reasoning history and Messages signatures travel between turns in an OS-encrypted Responses capsule. Incomplete streams are not marked successful. External HTTP 401/403/429 become local provider errors rather than ChatGPT authentication/quota signals. Measured Thread.modelProvider and excluded external chats from native thread ownership.
10. The live picker applied native maximum-consumption warnings to the highest of two external efforts. AST inspection found a unique modelPickerTriggerConfig/lockedModelSlug/powerSelections contract. Patch 123 removes ChatGPT speed controls and isMaximum-derived warnings only for external selections; native permissions remain intact.
11. Saving a dummy key in real Electron failed because electron_browser_safe_storage was unavailable. The Node fake-cipher test did not cover this build difference. Windows now uses DPAPI CurrentUser through fixed PowerShell code and stdin/stdout pipes. Keys never enter argv, environment variables, or temporary files; failures do not fall back to plaintext. Other platforms reject saves if their secure backend is unavailable.
12. The user requested inline Settings. AST inspection located the native visibility hook, Suspense content path, and navigation row. The cxp-providers route renders within Settings, directly after Analytics through native onSelect, with a shadow-root UI. Live order: usage → analytics → cxp-providers → computer-use. No separate provider-window target remained. Evidence: inline-settings.png, inline-usage.png.
13. Two dummy keys were saved with DPAPI in the final candidate, producing three test connections; the public view reported rawKeyExposed=false. Real mouse input in Settings switched ChatGPT Free → Pro and preserved three reset credits. The selected second external connection sent its key to the mock at high effort without ChatGPT-Account-ID. thread/read(includeTurns) returned list_turns is not supported yet; the isolated rollout instead confirmed EXTERNAL_OK and task_complete. Evidence: inline-rollout-result.json. The failed read was retained as a native API variation.
14. Passed 15 provider tests, including real Windows DPAPI, plus native auth/PE/startup/usage-window and pool/profile/reset/routing/weight matrices. Syntax checks passed for 63 JS files. All seven output bundles from 19 patches matched the packaged working files byte-for-byte. A Windows CI job was added; remote CI was not run.
15. Updated the installed CodexPP copy after backing up EXE/ASAR/hub to `%LOCALAPPDATA%/CodexPP/backups/providers-20260905104652Z`. Installed hashes matched the candidate. Reopened the installed EXE in an isolated profile: one Providers row below Analytics, three connections/editor in the same page, empty password input, no horizontal overflow, no separate provider window. Screenshot: docs/provider-settings.png.
16. Delayed installed-app logs contained zero auth 401/token-unavailable/account-mismatch/ReferenceError/TypeError events. CDP fromSurface:false correctly captured composited native translucent surfaces; application colors were not changed.
17. Closed only the process tree matching the installed EXE and isolated user-data path; stopped the mock. Restored test auth/accounts and removed only the three known dummy connections. Original auth/accounts and Store ASAR hashes stayed unchanged. No commit/push. At this stage, paid-provider requests and Go quota were still unverified because no user key was connected.
18. The user saved a Go key and reported broken model-list scrolling. Auth/accounts/providers were backed up under CodexPP-scroll-go-0905/backup. Only the encrypted provider file was copied into the isolated profile; gateway tests used gateway-data separately. Decryption remained local to DPAPI → request header, with no plaintext diagnostic output. The new profile initially showed onboarding; prior test UI state and the same elevated Windows sandbox test setting allowed Settings access. This did not complete Windows sandbox setup; its composer banner remained.
19. Reverified the AST-derived Settings route in live DOM. Providers → Edit showed 35 models, a 3002px host, 2359px model content, and a 784px viewport. The host/intermediate ancestors had overflow:visible, while native outer content had overflow:hidden. An unbounded editor produced no actual scroll container. Real CDP wheel deltaY=1200 left all scroll positions at zero; the last model was at y=2789. Evidence: scroll-before.json. This isolated a panel sizing contract failure rather than missing model data.
20. The inline host now uses native content height with height:100%, minHeight:0, and overflowY:auto. The model list has its own 180–460px scroll area; both containers contain overscroll. Other native pages were unchanged. Reapplied patch 122 to the original settings bundle, parsed its AST, and regenerated that bundle before updating candidate ASAR/PE integrity.
21. Live Go tests passed HTTP 200, response.completed, and GO_OK for DeepSeek V4 Flash Chat/low, MiniMax M2.7 Messages, and GPT-5.6 Luna Responses/low. DeepSeek/high and MiniMax also completed get_test_value → local result → GO_TOOL_OK_0905, retaining encrypted reasoning history. Evidence: go-live.json, go-tools.json. Go usage reported 100% rolling, 88% weekly, and 73% monthly remaining at that moment. These are quota percentages, not money; live tests consume Go quota.
22. The initial Desktop request-client turn failed with an unsupported namespace tool error. Thread: 01a07154-b9c6-7543-8362-b3ab3c1ba669. task_complete alone was not counted as success because it can carry a failure. An official app-server → local mock measurement found standalone functions, namespaced functions, and hosted web_search (desktop-tool-measurement.json, desktop-tools.json). No user key was used for schema measurement.
23. Chat/Messages namespace tools map to deterministic distinct names shorter than 64 characters; streamed results and history map back to the original name/namespace. Custom-tool input wrapping is preserved. OpenAI hosted web_search is disabled in these threads rather than imitated. Live namespace round trips passed for both protocols (go-tools-namespace.json). The Desktop retest completed with error=null and GO_DESKTOP_OK_0905: thread 01a0715c-c090-7671-930d-657866f2ed43, turn 01a0715c-c3d1-7441-bb2e-2482d256a61a; go-desktop-fixed-result.json.
24. After the scroll fix, the host measured 702px/996px content and the model list 353px/2365px. Real wheel input moved modelsTop 0 → 2012 and outer hostTop 145 → 293; the final model reached y=678 inside the viewport. At 900×651, the layout became one column with 35 models and no horizontal overflow. Evidence: scroll-after.json, scroll-outer-after.json, scroll-small.json, scroll-fixed.png. A screenshot forced a frame when the background Windows compositor delayed wheel handling; zero-movement samples were not passes.
25. Passed 17 tests. The first new config test inherited a default connection lacking its model; pinning its own thread connection made it independent. Syntax and normal diff checks passed. Windows shutdown initially left an install file locked; copying completed after process exit. Backup: CodexPP/backups/scroll-go-20260905114203Z. Installed ASAR/EXE matched the candidate, 26 hub files matched the repo, and auth/accounts/providers hashes stayed unchanged during installation.
26. Reopened the installed EXE in an isolated profile; provider bridge and native profile were ready. Delayed auth/token/account/JS-error counts were zero; Store ASAR stayed unchanged. Normal git diff --check passed. A diagnostic override of core.autocrlf=false incorrectly flagged existing CRLF lines; files were not reformatted. Closed the isolated process, restored its auth/accounts, and reopened normal Codex++. Original state hashes matched starting backups (scroll-originals.json).
27. Cleanup boundary: automatic approval review rejected deleting the three encrypted test provider copies under CodexPP-scroll-go-0905: data/providers.json, gateway-data/providers.json, backup/original-providers.json. It returned only blocked by policy. Deletion was not retried through another tool; the encrypted copies remain locally. The user was informed. No commit/push.
28. Follow-up tool and stale-quota investigation is documented in [provider-tools-and-usage-0905.md](provider-tools-and-usage-0905.md). The user confirmed English as the repository language. Newly added documentation, provider UI text, and safe errors were translated to English, with matching test expectations. Historical measurements and failed-test evidence were retained.
29. Installed the verified English/usage package with backup usage-isolation-20260905124604Z. All 19 patches/seven bundles and 26 installed hub files matched their final sources. Live verification in the normal user profile confirmed the inline English panel, inner/outer wheel scrolling, healthy-account quota refresh, and a separate expired-account error. Auth, saved credentials, provider keys/preferences, and Store ASAR were preserved. Restored isolated test auth/accounts and reopened normal Codex++ without CDP. See the linked follow-up log for the final quota snapshot, delayed-error checks, and remaining tool limitations.
30. The user then reported missing external models in the composer. Measured the real provider store: one enabled/ready Go connection, 35 supported models, zero enabled models. Read catalog() and the model/list merge: the enabled-model filter correctly produced zero external entries. This narrowed the issue to saved model visibility rather than credentials, discovery, or a missing picker patch.
31. Backed up the current auth/accounts/providers under model-visibility-2026-09-05T12-54-47-007Z, then enabled the connected Go models in response to the request. The catalog returned 35 entries; key and endpoint were unchanged. Restarted the installed app with its normal profile. Its own model/list returned the 35 external entries, and the actual picker contained 35 Go rows. The initial collapsed Select model section clipped those rows; expanding it exposed its native scroll container. This hidden DOM state was not counted as visible selection evidence.
32. Expanded Select model, scrolled to DeepSeek V4 Flash, and selected it with real CDP mouse input. Its radio row became aria-checked=true and the composer displayed that external model. The catalog offered low/high/max; the actual picker showed the corresponding three-position effort control. No completion was sent during this visibility test. Evidence: tools-usage/model-visibility.json, model-client-catalog.json, model-selected-state.json, and model-visible-final.json. The guide now explains zero-enabled-model state, saving selections, restart, and expanding the model list.
33. The user authorized direct publication to main. Fetch found upstream b99d281, containing the macOS 26.825 anchor fix. Reconciled the changelog and compatibility entries, retained its optional branding wrapper, and preserved model-aware routing. The incoming shape-test fixture was not valid JavaScript and failed the new AST lookup; wrapping it in actual dispatch/creation functions and asserting the selected-model argument made all seven shape cases pass. Existing Windows auth, PE integrity, startup, usage-window, and 18 provider/usage tests also passed. Temporary profiles, API keys, and ignored measurement artifacts are outside the publication scope.
34. Post-rebase verification applied all 19 patches and parsed their output. Six bundles matched the live-tested artifact byte-for-byte; Settings differed only in embedded asset line endings introduced by Git's Windows checkout conversion. Normalizing the three embedded text assets to LF in patch 122 restored exact equality for the seventh bundle. This keeps generated Settings assets deterministic across LF/CRLF checkouts without changing the live-tested UI.
