Follow the working rules in @AGENTS.md for every task in this repository.

The rule that overrides convenience: go from the most general to the most
specific, and log each step with the evidence that closed it. Do not start
editing code before the failing layer is identified by a measurement.
